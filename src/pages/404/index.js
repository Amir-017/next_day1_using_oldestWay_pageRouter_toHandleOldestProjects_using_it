export const Errorpage = () => {
  return (
    <div className="flex flex-col items-center justify-center h-screen"> Error 404: there is an Error Found </div>
  );
};

Errorpage.hideNavbar = true;

export default Errorpage;